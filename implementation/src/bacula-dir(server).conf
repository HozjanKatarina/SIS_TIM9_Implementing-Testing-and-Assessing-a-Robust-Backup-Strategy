#
# Default Bacula Director Configuration file
#
#  The only thing that MUST be changed is to add one or more
#   file or directory names in the Include directive of the
#   FileSet resource.
#
#  You might also want to change the default email address
#   from root to your address.  See the "mail" and "operator"
#   directives in the Messages resource.
#
# Copyright (C) 2000-2022 Kern Sibbald
# License: BSD 2-Clause; see file LICENSE-FOSS
#

Director {                            # define myself
  Name = ubuntu-server-dir
  DIRport = 9101                # where we listen for UA connections
  QueryFile = "/etc/bacula/scripts/query.sql"
  WorkingDirectory = "/var/lib/bacula"
  PidDirectory = "/run/bacula"
  Maximum Concurrent Jobs = 20
  Password = "vboxdir"         # Console password
  Messages = Daemon
  DirAddress = 127.0.0.1
}

Job {
  Name = "Son-Production-Job"
  Type = Backup
  Level = Incremental
  Client = kali-fd
  FileSet = "DummySet"
  Schedule = "Sched-Son"
  Storage = File1
  Pool = Son
  Messages = Standard
  Client Run Before Job = "/usr/local/bin/duplicity.sh %l Son"
}

Job {
  Name = "Father-Production-Job"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Schedule = "Sched-Father"
  Storage = File1
  Pool = Father
  Messages = Standard
  Client Run Before Job = "/usr/local/bin/duplicity.sh %l Father"
}

Job {
  Name = "GF-Production-Job"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Schedule = "Sched-GF"
  Storage = File1
  Pool = Grandfather
  Messages = Standard
  Client Run Before Job = "/usr/local/bin/duplicity.sh %l Grandfather"
}
 
# --- DEMO ---
Job {
  Name = "Demo-Son-Job"
  Type = Backup
  Level = Incremental
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Demo-Son
  Messages = Standard
  Client Run Before Job = "/usr/local/bin/duplicity.sh %l Demo-Son"
}
 
# --- DEMO ---
Job {
  Name = "Demo-Father-Job"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Demo-Father
  Messages = Standard
  Client Run Before Job = "/usr/local/bin/duplicity.sh %l Demo-Father"
}
 
# --- DEMO ---
Job {
  Name = "Demo-GF-Job"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Demo-Grandfather
  Messages = Standard
  Client Run Before Job = "/usr/local/bin/duplicity.sh %l Demo-Grandfather"
}

# --- Restore (Produkcija) ---
Job {
  Name = "Restore-Production-Son"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Son
  Messages = Standard

  Run Script {
    Command = "/usr/local/bin/duplicity.sh Restore Son"
    Runs When = Before
    Runs On Client = Yes
    FailJobOnError = Yes
  }
}
 
Job {
  Name = "Restore-Production-Father"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Father
  Messages = Standard

  Run Script {
    Command = "/usr/local/bin/duplicity.sh Restore Father"
    Runs When = Before
    Runs On Client = Yes
    FailJobOnError = Yes
  }
}
 
Job {
  Name = "Restore-Production-GF"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Grandfather
  Messages = Standard

  Run Script {
    Command = "/usr/local/bin/duplicity.sh Restore Grandfather"
    Runs When = Before
    Runs On Client = Yes
    FailJobOnError = Yes
  }
}

# --- Restore Demo ---
Job {
  Name = "Restore-Demo-Son"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Demo-Son
  Messages = Standard

  Run Script {
    Command = "/usr/local/bin/duplicity.sh Restore Demo-Son"
    Runs When = Before
    Runs On Client = Yes
    FailJobOnError = Yes
  }
}
 
Job {
  Name = "Restore-Demo-Father"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Demo-Father
  Messages = Standard

  Run Script {
    Command = "/usr/local/bin/duplicity.sh Restore Demo-Father"
    Runs When = Before
    Runs On Client = Yes
    FailJobOnError = Yes
  }
}
 
Job {
  Name = "Restore-Demo-GF"
  Type = Backup
  Level = Full
  Client = kali-fd
  FileSet = "DummySet"
  Storage = File1
  Pool = Demo-Grandfather
  Messages = Standard

  Run Script {
    Command = "/usr/local/bin/duplicity.sh Restore Demo-Grandfather"
    Runs When = Before
    Runs On Client = Yes
    FailJobOnError = Yes
  }
}

FileSet {
  Name = "DummySet"
  Include {
    Options { signature = MD5 }
    File = /var/lib/bacula/dummy.txt
  }
}
 
#
# Standard Restore template, to be changed by Console program
#  Only one such job is needed for all Jobs/Clients/Storage ...
#
Job {
  Name = "RestoreFiles"
  Type = Restore
  Client=ubuntu-server-fd
  Storage = File1
# The FileSet and Pool directives are not used by Restore Jobs
# but must not be removed
  FileSet="Full Set"
  Pool = File
  Messages = Standard
  Where = /nonexistent/path/to/file/archive/dir/bacula-restores
}
 

# List of files to be backed up
FileSet {
  Name = "Full Set"
  Include {
    Options {
      signature = MD5
    }
#
#  Put your list of files here, preceded by 'File =', one per line
#    or include an external list with:
#
#    File = <file-name
#
#  Note: / backs up everything on the root partition.
#    if you have other partitions such as /usr or /home
#    you will probably want to add them too.
#
#  By default this is defined to point to the Bacula binary
#    directory to give a reasonable FileSet to backup to
#    disk storage during initial testing.
#
    File = /usr/sbin
  }

#
# If you backup the root directory, the following two excluded
#   files can be useful
#

  Exclude {
    File = /var/lib/bacula
    File = /nonexistent/path/to/file/archive/dir
    File = /proc
    File = /tmp
    File = /sys
    File = /.journal
    File = /.fsck
  }
}
 
#
# When to do the backups, full backup on first sunday of the month,
#  differential (i.e. incremental since full) every other sunday,
#  and incremental backups other days
Schedule {
  Name = "WeeklyCycle"
  Run = Full 1st sun at 23:05
  Run = Differential 2nd-5th sun at 23:05
  Run = Incremental mon-sat at 23:05
}
 
# This schedule does the catalog. It starts after the WeeklyCycle
Schedule {
  Name = "WeeklyCycleAfterBackup"
  Run = Full sun-sat at 23:10
}
 
Schedule {
  Name = "GustiGFS"

  Run = Level=Full hourly at 00:00
  Run = Level=Incremental hourly at 00:01
  Run = Level=Incremental hourly at 00:02
  Run = Level=Incremental hourly at 00:03
  Run = Level=Incremental hourly at 00:04
  Run = Level=Full hourly at 00:05
  Run = Level=Incremental hourly at 00:06
  Run = Level=Incremental hourly at 00:07
  Run = Level=Incremental hourly at 00:08
  Run = Level=Incremental hourly at 00:09
  Run = Level=Incremental hourly at 00:10
}
 
#Finalni GFS ciklusi
# Raspored za Grandfather (1. ponedjeljak u mjesecu)
Schedule {
  Name = "Sched-GF"
  Run = Level=Full 1st mon at 23:00
}

# Raspored za Father (ostali ponedjeljci)
Schedule {
  Name = "Sched-Father"
  Run = Level=Full 2nd-5th mon at 23:00
}
 
# Raspored za Son (utorak - nedjelja)
Schedule {
  Name = "Sched-Son"
  Run = Level=Incremental tue-sun at 23:00
}
 
#Poolovi s definicijom zadržavanja zapisa u katalogu
Pool { 
  Name = Son; 
  Pool Type = Backup; 
  Volume Retention = 30 days;
  Label Format = "Son-" 
}
 
Pool { 
  Name = Father; 
  Pool Type = Backup; 
  Volume Retention = 365 days;
  Label Format = "Father-"
}
 
Pool { 
  Name = Grandfather; 
  Pool Type = Backup; 
  Volume Retention = 100 years;
  Label Format = "GF-" 
}
 
# Demo Son - čuva se samo 5 minuta (za brzu demonstraciju brisanja)
Pool {
  Name = Demo-Son
  Pool Type = Backup
  Volume Retention = 5 minutes
  Label Format = "Demo-Son-"
}
 
# Demo Father - čuva se 15 minuta
Pool {
  Name = Demo-Father
  Pool Type = Backup
  Volume Retention = 15 minutes
  Label Format = "Demo-Fa-"
}

# Demo Grandfather - čuva se 1 sat
Pool {
  Name = Demo-Grandfather
  Pool Type = Backup
  Volume Retention = 1 hour
  Label Format = "Demo-GF-"
}
 
# This is the backup of the catalog
FileSet {
  Name = "Catalog"
  Include {
    Options {
      signature = MD5
    }
    File = "/var/lib/bacula/bacula.sql"
  }
}
 
#testni set
FileSet{
  Name = "TestniSet"
  Include {
    Options {
      signature = MD5
    }
    File = /home/vbox/vazni_podaci
  }
}
 
# Client (File Services) to backup
Client {
  Name = ubuntu-server-fd
  Address = localhost
  FDPort = 9102
  Catalog = MyCatalog
  Password = "vbox"          # password for FileDaemon
  File Retention = 60 days            # 60 days
  Job Retention = 6 months            # six months
  AutoPrune = yes                     # Prune expired Jobs/Files

  TLS Enable = no
}
 
Client {
  Name = kali-fd
  Address = 10.0.2.15
  FDPort = 9102
  Password = "kali"
  Catalog = MyCatalog

  TLS Enable = Yes
  TLS Require = Yes
  TLS CA Certificate File = /etc/bacula/ssl/bacula-ca.crt
    TLS Certificate = /etc/bacula/ssl/ubuntu-server.crt
  TLS Key = /etc/bacula/ssl/ubuntu-server.key
}
 
Storage{
  Name = File1
  Address = 10.0.2.4
#localhost
  SDPort = 9103
  Password = "vbox"
#"c8e3aEZ8BbnIGe0x6qTVBxDzMydYc5HMM"
  Device = FileChgr1
  MediaType = File1
  Autochanger = yes
  TLS Enable = no
}
 
# Definition of file Virtual Autochanger device
Autochanger {
  Name = File12
# Do not use "localhost" here
  Address = localhost                # N.B. Use a fully qualified name here
  SDPort = 9103
  Password = "c8e3aEZ8BbnIGeOx6qTVBxDzMydYc5HMM"
  Device = FileChgr1
  Media Type = File1
  Maximum Concurrent Jobs = 10        # run up to 10 jobs a the same time
  Autochanger = File1                 # point to ourself
}
 
# Definition of a second file Virtual Autochanger device
#   Possibly pointing to a different disk drive
Autochanger {
  Name = File2
# Do not use "localhost" here
  Address = localhost                # N.B. Use a fully qualified name here
  SDPort = 9103
  Password = "c8e3aEZ8BbnIGeOx6qTVBxDzMydYc5HMM"
  Device = FileChgr2
  Media Type = File2
  Autochanger = File2                 # point to ourself
  Maximum Concurrent Jobs = 10        # run up to 10 jobs a the same time
}
 
# Generic catalog service
Catalog {
  Name = MyCatalog
  dbname = "bacula"; DB Address = "localhost"; dbuser = "bacula"; dbpassword = "vbox"
}

# Reasonable message delivery -- send most everything to email address
#  and to the console
Messages {
  Name = Standard
#
# NOTE! If you send to two email or more email addresses, you will need
#  to replace the %r in the from field (-f part) with a single valid
#  email address in both the mailcommand and the operatorcommand.
#  What this does is, it sets the email address that emails would display
#  in the FROM field, which is by default the same email as they're being
#  sent to.  However, if you send email to more than one address, then
#  you'll have to set the FROM address manually, to a single address.
#  for example, a 'no-reply@mydomain.com', is better since that tends to
#  tell (most) people that its coming from an automated source.

#
  mailcommand = "/usr/sbin/bsmtp -h localhost -f \"\(Bacula\) \<%r\>\" -s \"Bacula: %t %e of %c %l\" %r"
  operatorcommand = "/usr/sbin/bsmtp -h localhost -f \"\(Bacula\) \<%r\>\" -s \"Bacula: Intervention needed for %j\" %r"
  mail = root = all, !skipped
  operator = root = mount
  console = all, !skipped, !saved
#
# WARNING! the following will create a file that you must cycle from
#          time to time as it will grow indefinitely. However, it will
#          also keep all your messages if they scroll off the console.
#
  append = "/var/log/bacula/bacula.log" = all, !skipped
  catalog = all
}
 

#
# Message delivery for daemon messages (no job).
Messages {
  Name = Daemon
  mailcommand = "/usr/sbin/bsmtp -h localhost -f \"\(Bacula\) \<%r\>\" -s \"Bacula daemon message\" %r"
  mail = root = all, !skipped
  console = all, !skipped, !saved
  append = "/var/log/bacula/bacula.log" = all, !skipped
}
 
# Default pool definition
Pool {
  Name = Default
  Pool Type = Backup
  Recycle = yes                       # Bacula can automatically recycle Volumes
  AutoPrune = yes                     # Prune expired volumes
  Volume Retention = 365 days         # one year
  Maximum Volume Bytes = 50G          # Limit Volume size to something reasonable
  Maximum Volumes = 100               # Limit number of Volumes in Pool
}

# File Pool definition
Pool {
  Name = File
  Pool Type = Backup
  Recycle = yes                       # Bacula can automatically recycle Volumes
  AutoPrune = yes                     # Prune expired volumes
  Volume Retention = 365 days         # one year
  Maximum Volume Bytes = 50G          # Limit Volume size to something reasonable
  Maximum Volumes = 100               # Limit number of Volumes in Pool
  Label Format = "Vol-"               # Auto label
}
 

# Scratch pool definition
Pool {
  Name = Scratch
  Pool Type = Backup
}
 
#
# Restricted console used by tray-monitor to get the status of the director
#
Console {
  Name = ubuntu-server-mon
  Password = "gwfQayuh_S5EyeWb5ooNj4tj0qMkqG9V0"
  CommandACL = status, .status
}
